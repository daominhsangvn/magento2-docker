input {
	beats {
		port => 5044
		add_field => {
          "application_name" => "%{[docker][container][labels][log_name]}"
        }
	}

	tcp {
		port => 5000
	}
}

## Add your filters / logstash plugins configuration here

filter {
    grok {
        match => { "message" => "%{COMBINEDAPACHELOG}"}
    }
#     geoip {
#         source => "clientip"
#     }
}

output {
	elasticsearch {
		hosts => "op-elasticsearch:9200"
		user => "elastic"
		password => "changeme"
		ecs_compatibility => disabled
		index => "%{application_name}"
	}
}
