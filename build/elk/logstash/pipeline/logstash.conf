input {
	beats {
		port => 5044
		add_field => {
#           "container_name" => "%{[docker][container][labels][log_name]}"
          "service_name" => "%{[docker][container][labels][log_name]}"
          "service_group" => "%{[docker][container][labels][log_group]}"
        }
	}

	tcp {
		port => 5000
	}
}

## Add your filters / logstash plugins configuration here

filter {
    grok {
        match => { "message" => "%{COMBINEDAPACHELOG}"}
    }
#     geoip {
#         source => "clientip"
#     }
}

output {
	elasticsearch {
		hosts => "op-elasticsearch:9200"
		user => "elastic"
		password => "changeme"
		ecs_compatibility => disabled
		index => "%{service_group}_%{container_name}_%{service_type}"
	}
}
