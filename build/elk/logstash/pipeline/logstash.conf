input {
	beats {
		port => 5044
		add_field => {
          "index_name" => "%{[docker][container][labels][service_group]}_%{[container][name]}"
        }
	}

	tcp {
		port => 5000
	}
}

## Add your filters / logstash plugins configuration here

filter {
    grok {
        match => { "message" => "%{COMBINEDAPACHELOG}"}
    }
#     geoip {
#         source => "clientip"
#     }
}

output {
	elasticsearch {
		hosts => "op-elasticsearch:9200"
		user => "elastic"
		password => "changeme"
		ecs_compatibility => disabled
		index => "%{index_name}"
	}
}
